{
  "name": "ai-fake-news-detector",
  "version": "1.0.0",
  "description": "Flask backend wrapped in a Node environment for Render",
  "scripts": {
    "postinstall": "python3 -m venv venv && ./venv/bin/pip install -r requirements.txt",
    "start": "./venv/bin/gunicorn app:app"
  },
  "dependencies": {}
}
